# Core
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
pydantic>=2.6
pydantic-settings>=2.2

# Database
sqlalchemy>=2.0
aiosqlite>=0.20
alembic>=1.13

# LLM Director
llama-cpp-python>=0.2.75  # Build with CUDA: CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python

# Image Generation
diffusers>=0.28
transformers>=4.40
accelerate>=0.30
safetensors>=0.4
sentencepiece

# Video Generation (LTX)
# LTX Desktop has its own Python env — we call it via subprocess or import directly
# For Wan: pip install diffusers (already above)

# Upscaling
realesrgan>=0.3.0
basicsr>=1.4.2
gfpgan>=1.3.8  # dependency of realesrgan

# Music (ACE-Step)
# ACE-Step has its own requirements — install separately

# Audio
# whisperx — install via: pip install git+https://github.com/m-bain/whisperX.git

# Torch (CUDA 12.x)
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
torch>=2.3
torchvision
torchaudio

# Utilities
pillow>=10.3
opencv-python-headless>=4.9
numpy>=1.26
pyyaml>=6.0
httpx>=0.27
python-multipart>=0.0.9
aiofiles>=23.2
